# Writability — npm configuration
#
# Native modules (better-sqlite3) are compiled against Electron's ABI by the
# postinstall hook (`electron-builder install-app-deps`), which auto-detects the
# installed Electron version. That is more reliable than pinning a global
# `runtime=electron` target here, so we don't.
fund=false
audit=false
